WHAT CHANGED IN v0.10.112 TEST
- Fixed the floating "Resume conflict resolution" button covering delivery-docket editor controls.
- The floating resume control is now hidden while editing a docket.
- On the Dashboard, the floating resume control is hidden because the red correction card already provides its own Resume button.
- The TEST conflict helper now refuses to create a second unresolved synthetic conflict; if one already exists, it resumes that exact conflict instead.
- Existing guided conflict workflow/accounting logic is unchanged.
